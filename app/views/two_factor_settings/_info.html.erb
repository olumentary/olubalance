<% unless @enabled %>
  <div class="notification">
    <p>
      Two-factor authentication adds a second step at sign-in: a one-time
      6-digit code from an authenticator app on your phone.
      <strong>You can enroll multiple devices</strong> — useful if more than
      one person uses this account.
    </p>
  </div>
<% end %>
