<!DOCTYPE html>
<html lang="en" data-theme="light">

  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0"/>

    <title>oluhome</title>

    <%= render "shared/favicon" %>

    <% if content_for? :head %>
      <%= yield :head %>
    <% end %>

    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body data-controller="shared">

    <!--#####################################################################--
    #
    #                             To Malintha
    #
    #          Without you I would have never created this application.
    #         You are my inspiration, my motivation, and my best friend.
    #                     I love you with all of my heart.
    #
    #                                Love,
    #                                Kevin
    #
    #########################################################################-->

    <div id="app">
      <header>
        <%= render "shared/navbar" %>
      </header>

      <main class="olubalance site-content">
        <section class="section">
          <div class="container">
            <div id="flash_messages"></div>
            <%= yield %>
          </div>

          <% flash.each do |message_type, message| %>
            <% if message.present? and message.is_a? String %>
              <div 
                data-controller="shared" 
                data-shared-message-type="<%= message_type %>" 
                data-shared-message="<%= message %>"
              >
              </div>
            <% end %>
          <% end %>
        </section>
      </main>

      <footer class="footer">
        <%= render "shared/footer" %>
      </footer>
    </div>
  </body>

</html>
